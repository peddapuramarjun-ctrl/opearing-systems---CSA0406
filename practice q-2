#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>
#include <sys/wait.h>

int main()
{
    if (fork() == 0)
    {
        printf("Child Process\n");
        exit(0);
    }
    else
    {
        wait(NULL);
        printf("Child Completed\n");
    }

    return 0;
}
