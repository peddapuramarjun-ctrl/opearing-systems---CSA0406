#include <stdio.h>
#include <fcntl.h>
#include <unistd.h>

int main()
{
    int fd = open("student.txt", O_CREAT | O_WRONLY, 0644);

    if(fd >= 0)
        printf("File Created");

    close(fd);
    return 0;
}
