#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>

int main()
{
    for (int i = 0; i < 3; i++)
    {
        if (fork() == 0)
        {
            printf("PID = %d  PPID = %d\n", getpid(), getppid());
            exit(0);
        }
    }

    return 0;
}
