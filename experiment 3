#include <stdio.h>

int main() {
    int n, bt[10], wt = 0;

    scanf("%d", &n);

    for (int i = 0; i < n; i++)
        scanf("%d", &bt[i]);

    for (int i = 0; i < n; i++) {
        printf("P%d WT=%d TAT=%d\n", i + 1, wt, wt + bt[i]);
        wt += bt[i];
    }

    return 0;
}
